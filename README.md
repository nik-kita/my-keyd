# [default.conf](./default.conf)

```yaml

[ids]
*
[main]
capslock = overload(capslock_layer, esc)
j+k = esc
leftshift+rightshift = capslock

[capslock_layer]

h = left
j = down
k = up
l = right
enter = macro(C-r)
space = macro(C-b)

# P.S.
# do not copy from there (use default.conf intead) this is preview, demo and may contain mistakes or be out of date
```
